{
  "name": "fitcheck",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension MVP for personalized fashion fit recommendations.",
  "type": "module",
  "scripts": {
    "api": "node src/backend/server.js",
    "build": "npm run validate",
    "build:store": "node scripts/build-store-package.mjs",
    "dev": "npm run validate",
    "test": "node --test tests/*.test.mjs",
    "test:smoke": "node --test tests/smoke/smoke.test.mjs",
    "validate": "node scripts/validate-extension.mjs"
  },
  "devDependencies": {
    "puppeteer": "^22.15.0"
  }
}
